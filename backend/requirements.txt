# Core FastAPI dependencies
fastapi==0.115.12
uvicorn[standard]==0.34.2
python-multipart==0.0.9

# Database
mysql-connector-python

# Firebase
firebase-admin

# HTTP requests
requests

# Data processing
pandas
openpyxl

# Scheduling
apscheduler

# Web scraping (if needed)
beautifulsoup4

# Optional: Databutton (used as fallback for secrets, but env vars are primary)
# Can be removed if you only use environment variables - code will work without it
# databutton==0.38.34

# Environment variables
python-dotenv
